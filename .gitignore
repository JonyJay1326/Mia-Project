# 依赖
node_modules/

# 构建产物
dist/
dist-ssr/
.vite/

# 环境变量本地覆盖
.env.local
.env.*.local

# 数据库（含真实成长数据，绝不提交）
*.db
*.db-shm
*.db-wal
server/data/

# 日志
logs/
*.log
npm-debug.log*

# 编辑器 / 系统
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 本地临时
*.local

# 部署（根目录 tar 包忽略；CI 用 dist-release/ 以免 scp-action 跳过 gitignore 文件）
mia-release.tar.gz
deploy/.env.deploy
# 服务器 CI 上传目录（若误同步到本机）
.ci-upload/
