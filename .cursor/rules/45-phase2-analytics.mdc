---
description: 阶段二任务清单 - 崩溃统计分析
alwaysApply: false
---

# 阶段二任务清单

范围：**崩溃统计页** + 可选 AI 解读。样本 n&lt;5 时只提示「先多记」，不下结论。

- [x] **P2-1** `GET /api/analytics/meltdown?days=`：周趋势 / chips / 触发 / 地点 / 应对 / 午睡 / **高发时段**
- [x] **P2-2** 前端 `/analysis`：概览 + 自绘条形图 + 区间 30/60/90 天
- [x] **P2-3** 侧边栏「分析」；手机底栏仍只保留录入相关页
- [x] **P2-4** 高发时段；空状态引导；示例崩溃种子；快捷键 `A`
- [x] **P2-AI** 分析页「生成解读」（`POST /api/ai/insight` + Mia 档案；不写回 events）
- [ ] **P2-5** 用真实崩溃记录替换/删掉种子数据后复验

**有意砍掉（勿再加回）：** 按星期排行、在场照护人排行（`byWeekday` / `byCaregiver`）。

地点标签含：家里 / 户外 / 商场 / 爷奶家 / 桃树地 / 彤彤姐家 / 学校 / 其他。

明确不做：因果文案自动写回 events、ECharts 全家桶。
