---
description: Mia 的成长中心 - 项目定位、阶段边界、核心铁律
alwaysApply: true
---

# Mia 的成长中心 · 核心铁律

给女儿 Mia（2024-05-17 出生）做的成长记录系统。**主战场是电脑端浏览，手机端只用于快速录入。**

## 📖 规格文档（动手前必读）

项目根目录 `Mia的成长中心-阶段一实施规格.md` 是唯一权威依据，含完整 schema、API、录入页设计、设计 Token、任务清单。

**改任何代码前，先读规格文档对应章节。** 文档里写了「别做」的事，一律别做。

## 🎯 当前阶段：阶段三（相册 + 技能地图 + AI 咨询）

- 📷 相册 · 🌱 技能地图 · 🤖 **AI 咨询（DeepSeek，必带 Mia 档案）**
- 判断只在咨询侧产出，不写回事件/语录库

**明确后置：**

| 后置 | 原因 |
|---|---|
| ❌ 应用内登录 | 改用 Nginx Basic Auth，见规格 8.2 |
| ❌ Service Worker | 无域名无 HTTPS 做不了，见规格 7.1 |

## ⚠️ 关键约束

- **服务器只有 3.32 GB 内存**，且已跑着「班主任管理平台」一个站点。内存是瓶颈不是 CPU
- **无域名，IP + HTTP 访问**（域名后补，见规格 8.3）
- **不做离线优先框架**（Dexie 全家桶等），只需一个简单草稿队列

## 💡 设计原则

- **能用交互设计解决的，别用 AI 解决** —— 录入页的便捷靠场景卡片 + chips，不靠语音识别
- **只记可观测事实，不记判断分析** —— 分析是消费侧的产物，数据要保持纯净
- **权威数据在用户自己的文件里** —— 不依赖 AI 隐式记忆
