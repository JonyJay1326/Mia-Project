---
description: 柔和糖果风视觉规范 - 设计 Token、组件要求、四个坑
globs: ["**/*.vue", "**/*.css", "**/*.scss", "**/tokens.css", "**/element-override.css"]
alwaysApply: false
---

# 视觉风格：柔和糖果风

给 2 岁小女孩做的成长记录，**第一眼要感觉「活泼糖果」，不是「后台管理系统」**。

⚠️ **Element Plus 默认是后台风**（方角 4px、`#409EFF` 商务蓝、1px `#DCDFE6` 灰描边、无动效）。
专业但像在填工单。必须按下面改造，且要在 T1.5 一次性建好 token。

## 设计 Token

```css
:root {
  /* 底色：奶油，不是纯白也不是亮黄 */
  --c-cream:   #fffbf0;   /* 页面 */
  --c-cream-2: #fffdf8;   /* 卡片 */
  --c-cream-3: #f5ead6;   /* 次级 */

  /* 四色（已降饱和 15–25%，别往回调） */
  --c-coral: #e8736b;   /* 珊瑚红：主色 */
  --c-honey: #f5c45e;   /* 蜜糖黄：点缀 */
  --c-mint:  #7fc8a9;   /* 薄荷绿：辅助 */
  --c-sky:   #6ba3d6;   /* 天空蓝：辅助 */
  --c-grape: #a98bc4;   /* 葡萄紫：语录 */

  /* 墨色 */
  --c-ink:   #4a3f38;   /* 主文字 */
  --c-ink-2: #96867a;   /* 次要 */
  --c-ink-3: #b8a99c;   /* 弱化 */

  /* 描边与阴影 */
  --stroke: 2.5px solid #6b5a4e;          /* ⭐ 暖深褐，不是纯黑 */
  --shadow-sticker: 2.5px 2.5px 0 #e0cdb8;

  /* 圆角与动效 */
  --r-sm: 10px; --r-md: 12px; --r-lg: 14px; --r-pill: 999px;
  --ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1);
  --dur: 0.22s;
}
```

## 事件类型配色

崩溃 `#e8736b` 🍭 / 高光 `#e8b84a` ✨ / 技能 `#7fc8a9` 🌱 / 日常 `#c4a574` 📒 / 情绪 `#e8a87c` 🫧 / 吃喝拉撒睡 `#d4896a` 🥣 / 社交 `#7eb8b2` 👋 / 医疗 `#6b8fd6` 💊 / 语录 `#a98bc4` 💬  
（历史：睡眠 `#6ba3d6` 🌙）

Legacy 仅展示：`health` / `question`。真相源：`typeChip.ts`。

## ⚠️ 四个坑

1. **描边用暖深褐 `#6b5a4e`，不是纯黑 `#2d2d2d`** —— 纯黑 + 粗线 = 廉价卡通感且刺眼
2. **饱和度别回调** —— 四色已降 15–25%，是孩子妈觉得「原版太亮」后的优化结果
3. **描边不是处处加** —— 卡片/chips/按钮加；正文、分隔线、表格线不加。全加显乱
4. **别堆卡通素材** —— 可爱 = 圆润 + 柔和 + 会动，不是塞满插画

## 组件要求

| 组件 | 要求 |
|---|---|
| 卡片 | 14px 圆角 + 2.5px 描边 + 硬阴影，奶油底 |
| 按钮 | 胶囊形 999px + 2.5px 描边 |
| chips | 胶囊形 + 2px 描边 |
| 输入框 | 12px 圆角 + 2.5px 描边 |
| 抽屉 | 16px 圆角 + 3px 描边 |
| 图标 | **用 emoji**，不用 Element Plus 线性图标（后者细描边偏商务） |

**不用 `<el-tag>`**（方角 + 无描边，跟糖果风冲突）→ 自己写 `<TypeChip>` 胶囊组件。

**不用 `<el-card>`** 做场景卡片 → 自己写（要大 emoji + hover 弹起）。

Element Plus 用 CSS 变量覆写即可全局生效，见规格 1.2 节。
