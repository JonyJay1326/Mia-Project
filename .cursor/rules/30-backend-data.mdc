---
description: 后端 NestJS 与 SQLite 数据约定 - 幂等、时间戳、月龄、表结构
globs: ["server/**/*.ts", "server/**/*.sql", "src/api/**/*.ts", "src/types/**/*.ts"]
alwaysApply: false
---

# 后端与数据约定

## 表结构

权威：`server/src/db/schema.sql`。

| 表 | 说明 |
|---|---|
| `events` | 事件（**type 无 quote**） |
| `quotes` | 语录 |
| `photos` | 相册 |
| `skills` | 技能目录（`is_custom`、`created_at`） |
| `skill_marks` | 掌握标记（无行 = todo） |
| `ai_chats` | AI 咨询会话（messages JSON） |

**`events.type`：** `meltdown | skill | daily | emotion | sleep | diet | social | medical`  
Legacy 只读：`health | question`。

**地点：** `home | outdoor | mall | grandparents | taoshudi | tongtong | school | other`  
**记录人：** 类型含 `grandma|grandpa`，**录入仅 mom/dad**。

## ⚠️ POST 必须幂等（最容易踩的坑）

离线重放必然重复提交。用客户端生成的 `id`（`createId()`，HTTP 非 localhost 也可用）做主键：

```sql
INSERT OR IGNORE INTO events (...) VALUES (...)
```

## 时间戳

- **统一 ISO 字符串**（`new Date().toISOString()`）
- 别用 SQLite 的 DATETIME，时区会坑
- 别存 Unix 时间戳

## 月龄计算

**按完整月算，不是天数除 30。** Mia 出生日期 `2024-05-17`。

```typescript
function monthAge(birthDate: string, at: string): number {
  const b = new Date(birthDate), a = new Date(at)
  let m = (a.getFullYear() - b.getFullYear()) * 12 + (a.getMonth() - b.getMonth())
  if (a.getDate() < b.getDate()) m--
  return m
}
```

`month_age` 在 events 和 quotes 里都是**冗余字段**。

## 可空字段设计

崩溃详情（`intensity` / `duration_min` / `coping` / `outcome`）**全部可空**。  
`napped`（当日是否午睡）用来验证「睡了 vs 没睡」的情绪差异。

## 语录约定

- `content` 原话逐字，不加工
- `note` 是我的解读，跟 `content` 分开
- 展示**按月龄分组，不按日期**

## API 约定

统一响应 `{ ok: boolean, data?: T, error?: string }`。全局前缀 `api`。

```
POST/GET/PATCH/DELETE /events
POST/GET/PATCH/DELETE /quotes   （GET ?random=1 随机一条）
GET    /health
GET    /analytics/meltdown?days=60   # 无 byCaregiver / byWeekday

POST/GET/DELETE /photos
GET    /photos/:id/file?v=thumb|original

GET    /skills
POST   /skills                       # 自定义
PUT    /skills/:id/mark              # todo|emerging|done，可带 note
# PATCH /api/skills/:id/note、DELETE /api/skills/:id（仅自定义）

GET    /ai/status
POST   /ai/chat
GET/DELETE /ai/chats、GET /ai/chats/:id
POST   /ai/insight
POST   /ai/skill-suggest
POST   /ai/scene-suggest

GET    /auth/status
POST   /auth/login
GET    /auth/me
```

**公网主挡 Nginx Basic Auth**；可选 `MIA_AUTH_*` 应用内 Bearer。  
照片落盘 `server/data/photos/`，别把 API 端口裸露公网。  
技能目录 `skill-catalog.ts` 启动 upsert；档案已知项幂等标 done。

前端：`DELETE`/无 body **不要**默认 `Content-Type: application/json`。

AI 默认 DeepSeek（`server/.env`）：

```
MIA_AI_API_KEY=sk-...
MIA_AI_BASE_URL=https://api.deepseek.com/v1
MIA_AI_MODEL=deepseek-chat
# MIA_ARCHIVE_PATH=指向 Mia档案.md（可选）
```
