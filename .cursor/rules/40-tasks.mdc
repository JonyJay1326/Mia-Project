---
description: 阶段一任务清单 T1-T19 与执行顺序 - 问「下一步做什么」时读这个
alwaysApply: false
---

# 阶段一任务清单

**严格按顺序做。** 详细规格见根目录 `Mia的成长中心-阶段一实施规格.md` 第 9 节。

做完一项就把下面的 `- [ ]` 改成 `- [x]`，保持进度可见。

## 基础（按顺序，别跳）

- [x] **T1** 初始化 Vite + Vue3 + TS，配 Pinia、Element Plus 按需引入
- [x] **T1.5** ⭐ **视觉风格基建**（规格 1.2 节）：`tokens.css` + `element-override.css` + `<TypeChip>` + 基础组件样式
      ⚠️ **这步不做，后面所有页面都会长成后台管理系统**
- [x] **T2** 后端 NestJS + FastifyAdapter + better-sqlite3，建表 + `/health`
- [x] **T3** `types/event.ts` 前后端共用类型
- [x] **T4** 后端 CRUD（POST 幂等 + GET 分页）
- [x] **T5** 响应式骨架：电脑端侧边栏 + 手机端 TabBar（断点 ≥1024 / <768）
- [x] **T6** 录入页：场景卡片组件（读 config，按 count / 拖拽排序；**v3 类型入口卡**）
- [x] **T7** 录入页：chips + 字段表单 + 记住上次记录人（**仅爸妈**）/ 地点
- [x] **T8** 离线队列：IndexedDB 封装 + 失败存本地 + 启动时重放
- [x] **T9** 时间线页：月份导航 + 事件流 + 类型筛选 + 右侧抽屉详情（可删）
- [x] **T10** PWA manifest + 图标（192/512）；SW 配置写好但**默认关闭**
- [ ] **T11** 部署到 1Panel（IP + HTTP + Basic Auth），电脑端实测浏览 + 手机端实测录入耗时

> 阶段二/三进度见 `45-phase2-analytics.mdc`、`46-phase3-album.mdc`、`47-skills-map.mdc`、`48-ai-consult.mdc`。

## 渺言妙语（语录模块）

- [x] **T12** 建 `quotes` 表 + CRUD（GET 按月龄分组返回）
- [x] **T13** 录入页：输入框自动聚焦 + 上下文折叠 + 保存后回到录入态 + 复用离线队列
- [x] **T14** 语录墙：按月龄分组 + 卡片（原话大字号、中文引号「」、葡萄紫描边款、解读小字灰）
- [x] **T15** 时间线合并显示语录（前端按时间归并两个列表）
- [x] **T16** 快捷键 `Q` 直达语录录入 + `?` 帮助面板

## 互动形象（排在语录之后，气泡内容依赖 quotes 表）

- [x] **T17.1** 用规格 9.1.1 的脚本从 `_source.png` 切出 4 张透明图 + 统一画布底部对齐
- [x] **T17.2** `useSpriteState()` + `BlanketSprite.vue` / `SpriteImage.vue` / `QuoteBubble.vue`
      ⚠️ 语录没数据时气泡是空的，quotes 为空时隐藏气泡只留精灵

## 以后买了域名再做（现在别碰）

- [ ] **T18** 域名解析 + Let's Encrypt 证书 + 强制 HTTPS
- [ ] **T19** `VITE_ENABLE_SW=true` 重新构建，开启 Service Worker

## ✅ 验收标准

- 手机端从打开到保存 ≤ 15 秒
- 请求失败时存本地，联网自动补交（不依赖 Service Worker）
- **视觉验收：第一眼是「活泼糖果」，不是「后台管理系统」**
  - 卡片/chips/按钮有 2–2.5px 暖深褐描边（`#6b5a4e`），不是 1px 灰线
  - 按钮和 chips 是胶囊形（999px），卡片圆角 14px
  - 底色是奶油 `#fffbf0`，不是纯白
  - 四色撞色可见但不刺眼
  - hover 弹起 + 硬阴影、点击回弹、列表蹦入
- ⚠️ 阶段一**不验证**「断网能打开页面」（无域名无 HTTPS，能力暂缺）
